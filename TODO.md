# TODO
- [x] Update `src/utils/assets.js` to dynamically discover available media folders under `src/assets`.
- [ ] Implement robust folder label resolver (case-insensitive, trims spaces, normalize).
- [x] Ensure `getAssetsForFolder()` returns media for UI option labels (Topbar/Sidebar/BottomBoxes).

- [ ] Run `npm test` or `npm start` to verify clicking options updates the screen media.


